# Implementation Activity

- 2026-07-13 14:23 CEST — Final unit 9 review fixes: central read executor now returns safe Content-Type metadata, bounds metadata/session bodies (1 MiB/2 MiB), closes every body, and classifies read failures without retaining oversized content. Active `/turn` retry telemetry is offset at active-turn start so prior process attempts are excluded; idle/last remains process-observed. Semantic retry labels are sanitized before journal append. Added Content-Type, limits/secret-tail, failing-reader, per-turn offset, and journal-canary tests. No binary rebuilt.

- 2026-07-13 14:20 CEST — Unit 8 review fixes: export now derives an explicit selected active/last/no-turn workspace-conversation-turn scope and filters all retained events to that scope; active exports include matching post-checkpoint events, pruned chains retain safe matching suffixes, corrupt chains fail closed only event inclusion, and manifest/message scope policies are explicit. Tagged messages from another conversation are excluded; untagged current history is marked unproven. Content hashes are domain-separated and omitted for short/credential-like bodies. Search now reports overall availability separately from journal/runtime/message health, retains runtime/message search through journal failures, and fully orders field/result ties. Added interleaved conversation, active suffix, pruned/corrupt, metadata scope, hash, and deterministic tests. No binary rebuilt.

- 2026-07-13 14:07 CEST — Implemented roadmap unit 8: typed local `/search <query> [--json] [--limit N]` and `/export [path] [--json]`. Search is read-only/no-network/no-write, bounded and deterministic, and searches only redacted semantic journal summaries plus safe runtime/workspace/app/turn/tool and conversation message metadata; prompt and assistant bodies are excluded. Stable `SearchSchemaVersion: 1` results include source/type/id/time/matched field/safe preview/score and explicit journal health. Export creates a distinct deterministic `ExportSchemaVersion: 1` allowlisted ZIP (`config`, `context`, `events`, `journal`, `messages`, `status`, `turn`, manifest), redacts/omits message bodies while retaining hashes/counts, bounds events/messages/bytes, uses immutable active snapshots, and publishes atomically without overwrite to a 0700 profile-local `exports/` directory or conservative symlink-safe relative custom path. Added registry/help/validation, deterministic JSON/archive/member/hash/mode, query redaction/bounds, journal failure, metadata omission, canary, permissions, traversal/symlink, concurrency, and active-snapshot tests. No binary rebuilt.

- 2026-07-13 13:54 CEST — Implemented roadmap unit 7: typed local `/support-bundle [path] [--json]` creating deterministic stored ZIP archives with `SupportBundleSchemaVersion: 1`, atomic temp+rename, manifest SHA-256/size records, fixed member allowlist, normalized timestamps/order/modes, default private profile-local `support/` destination and safe relative custom paths. Bundles contain only redacted `/status`/`/turn` equivalents, bounded redacted event summaries, journal health, inventory/hashes, safe labels/config, and Go environment labels; no network/remote writes, credentials/session/browser/raw logs/prompts/attachments/workspace files, or recursive directories. Added focused deterministic archive, manifest/order/mode, default/custom path, no-overwrite/symlink/traversal, active snapshot, journal failure/cap, and redaction canary tests. No binary rebuilt.

- 2026-07-13 13:35 CEST — Review hardening for unit 5: active `/status` now treats nil/empty app, working-set, MCP inventory, and absent snapshot host as authoritative rather than falling back to mutable client fields. Hardened snapshot/status sanitation across all free-form fields; MCP ID/name/transport/source/URL are sanitized before sorting and hashes/generations are derived. Added active-empty snapshot, all-field status redaction, and MCP snapshot/hash ordering redaction regressions. No network/write behavior changed.

- 2026-07-13 13:26 CEST — Implemented roadmap unit 5: typed `/status` dispatch with validated `/status --json` completion/help, compact human health view, and stable schema-versioned JSON (`schemaVersion: 1`). Status is read-only/offline-safe (no network/auth/state writes), uses the immutable active turn snapshot or safe idle preview, sorts server/timeout output deterministically, and explicitly reports healthy/degraded/unknown/error states. It covers profile/normalized host, transport/auth labels, connection/context/turn snapshot, MCP/WDF, working set, effective runtime/policy, journal checkpoint health/recovery, and last-known metadata/build readiness. Added focused registry/help/validation, human/JSON determinism, no-HTTP/redaction, active snapshot, and journal missing/behind/corrupt/no-mutation tests. README and implemented spec updated. No binary rebuilt.

- 2026-07-13 13:08 CEST — Implemented roadmap unit 4: immutable, deep-copied, redaction-safe Nirvana `TurnRuntimeSnapshot` captured at the accepted message boundary. Migrated active Nirvana message payload consumers for conversation ID, history, IDE context, app scope, working set, and MCP inventory; semantic `turn_accepted` now records snapshot working-set identity, transport, and deterministic MCP generation safely. Captures normalized instance/profile, auth capability labels only, workspace/app state, effective model/skill and timeout configuration, and CLI build reference. Added focused deep-copy, boundary, active-mutation isolation/next-turn refresh, deterministic working-set/MCP, semantic-context, and credential-absence tests. Connection handshake, legacy web-gateway/Code Assist payloads, transport retry behavior, and future diagnostics remain deliberately deferred. No binary rebuilt.

- 2026-07-13 12:43 CEST — Corrected unit 3 checkpoint semantics and interrupted-tail append safety: recovery now reduces only post-checkpoint events, requires a safe connection/turn-accept boundary before replaying unknown lifecycle state, and computes incomplete status only from that suffix. Appending after a crash-truncated JSONL tail first verifies and truncates only the incomplete final record, preventing joined corrupt/valid JSON. Added regression coverage for checkpointed mid-turn retained history, invalid mid-turn suffixes, and repaired append/recovery. No binary rebuilt.

- 2026-07-13 12:43 CEST — Corrected unit 3 retention recovery: startup now replays a validated ordered archive-plus-live journal chain, tolerates only exact archive/live duplicate overlap from an interrupted rotation, and rejects conflicts/gaps. Snapshot-less recovery after retention requires sequence 1 to remain available; after archive pruning, snapshots must checkpoint at or after the first retained record or recovery fails explicitly. Added archive-chain, checkpoint, malformed-middle-line, five-archive cap, overlap, and gap tests. No binary rebuilt.

- 2026-07-13 12:43 CEST — Implemented roadmap unit 3: append-only, fsync-flushed local JSONL semantic journal with versioned workspace/conversation envelopes, safe 0600/0700 permissions, credential rejection before append, bounded private archive retention, and startup snapshot recovery/replay. Snapshot format remains compatible with one optional local checkpoint. Trailing partial journal lines recover safely; malformed middle records fail visibly; duplicate semantic events retain reducer idempotence; incomplete accepted/started turns are deterministically abandoned locally without changing ServiceNow. Added focused journal tests for append/replay, permissions, trailing corruption, behind snapshots, redaction rejection, retention/archive, duplicate conflicts, and incomplete turns. No binary rebuilt.

- 2026-07-13 12:00 CEST — Implemented the typed slash-command registry: registry-derived help/dispatch/suggestions/capture behavior, aliases and validation, runtime and active-turn availability enforcement, client-aware interactive menu filtering, and focused registry/availability/help tests.
- 2026-07-13 12:07 CEST — Implemented roadmap unit 2: versioned, redaction-safe internal semantic event envelope; deterministic pure turn reducer with terminal/usage/context/telemetry invariants and replay tests; and an observational Nirvana websocket compatibility seam. Existing rendering, persistence, telemetry, and transport behavior remain unchanged.
- 2026-07-13 12:23 CEST — Hardened unit 2: reducer-owned maps/slices clone before every Apply; immutable replay and failure tests; lifecycle-unique semantic event IDs with per-turn sequence; explicit Nirvana-only begin seam; server TurnID binding/mismatch validation; replay, terminal, usage, tool, and metadata safety invariants; integration-level Nirvana lifecycle/non-Nirvana gate coverage; and turn-end semantic ordering for observed usage/context before assistant/turn completion. No binary rebuilt.

- 2026-07-13 13:37 CEST — Implemented roadmap unit 6: typed read-only `/turn` and explicit local `/debug` controls. `/turn`/`--json` use the immutable active snapshot when present and reducer-owned last state otherwise; `schemaVersion: 1` covers lifecycle, server ID, snapshot/generation, timing, sorted tools, elicitation, usage, terminal/error, staged next context, retry/fallback/telemetry, and journal health. `/debug` defaults off with local `status|on|off`; `tail` (default 20, cap 100) and `event` inspect only the retained journal chain with deterministic JSON and explicit missing/corrupt/pruned conditions. Added redacted, allowlisted event views that omit assistant content and do not read/write network or local state during inspection. Added focused registry/help/completion/validation, no/last/active turn, JSON determinism, debug transitions, bounded order/cap, lookup/missing, journal failures, no-write/no-HTTP, redaction, and snapshot consistency tests. No binary rebuilt.

- 2026-07-13 13:50 CEST — Unit 6 review fixes: `/turn` availability is now explicitly `no_turn`, `active`, or `last`; terminal start/terminal timestamps and duration come only from a complete read-only archive-plus-live lifecycle chain, freezing terminal duration while active duration advances. Missing/corrupt/pruned timing remains unknown. Sanitized command-controlled `/debug` errors, made human event details compact deterministic JSON, and decode retained event payloads before allowlisted redacted rendering. Added timing, active-vs-terminal, pruned/missing, error-redaction, and human-detail ordering regressions. No binary rebuilt.
